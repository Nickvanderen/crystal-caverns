<section id="home-page">
  <h1>Crystal Caverns Secret Notes</h1>
  <% @posts.each do |post| %>
  <h3><%= link_to post.title, post %></h3>
  <p><%= post.created_at.strftime("%B, %d, %Y") %></p>
</section><!-- end home page -->
<% end %>
